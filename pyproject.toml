[project]
name = "stock-screener"
version = "0.1.0"
description = "Pre-market stock gap screener for US markets (TradingView scanner API)"
authors = [
    { name = "Hermes Agent" }
]
readme = "SKILL.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "flask>=3.1",
    "jinja2>=3.1",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
